{
  "name": "quill-image-uploader",
  "version": "0.0.1",
  "description": "A TS library to upload images in the quill editor to the server instead of being base64 encoded",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nelson Isioma",
  "license": "ISC",
  "dependencies": {
    "quill": "^1.3.6"
  },
  "devDependencies": {
    "@types/quill": "^2.0.9"
  }
}
